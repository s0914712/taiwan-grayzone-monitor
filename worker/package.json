{
  "name": "line-attendance-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LINE 到工統計助手（Cloudflare Worker）",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  }
}
