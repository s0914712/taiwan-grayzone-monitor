# src/ — Python Data Pipeline

## Pipeline Execution Order
```
fetch_ais_data.py → fetch_gfw_data.py → match_sar_ais.py
→ detect_ship_transfers.py → analyze_suspicious.py → exercise_prediction.py
→ extract_all_routes.py → generate_dashboard.py
```

## Files

### Core Pipeline (executed by GitHub Actions)

| File | Purpose | Input | Output |
|------|---------|-------|--------|
| `fetch_ais_data.py` | Fetch AIS from Taiwan Port Bureau (SOCKS5 proxy), update profiles, save tier-1/tier-2 tracks, detect identity changes | Port Bureau API | `data/ais_snapshot.json`, `docs/vessel_profiles.json`, `docs/ais_track_history.json`, `docs/ais_track_commercial.json`, `data/identity_events.json` |
| `fetch_gfw_data.py` | Fetch GFW SAR satellite detections for dark vessels + fishing hotspots | GFW API (`GFW_API_TOKEN`) | `dark_vessels.json`, `sar_detections.json` (full-precision unmatched detections for re-matching) |
| `fetch_s1_passes.py` | Query NASA CMR (Earthdata) for real Sentinel-1 IW-GRD acquisition times over the Taiwan bbox (35-day window, asc/desc + platform, frames ≤30min apart clustered into passes). Anonymous CMR search works; `EARTHDATA_TOKEN` sent as Bearer when set. **Fallback**: on CMR failure/empty, queries the CDSE OData catalogue (anonymous) and parses times from product names. 12h freshness check; failures keep the previous file. | CMR API (`EARTHDATA_TOKEN` optional), CDSE OData | `s1_pass_times.json` |
| `match_sar_ais.py` | **SAR × local-AIS re-matching** — de-noise GFW dark detections with the denser Port Bureau AIS: fixed-infrastructure filter (**cross-run recurrence** via `data/sar_detection_history.json` at 0.03° / ≥3 overpass dates — single-window detections never repeat a cell, so recurrence only emerges across cron runs — **plus** the committed `data/fixed_infrastructure.json` static wind-farm mask), dead-reckoning interpolation to Sentinel-1 overpass time, dynamic gating radius (speed×Δt + SAR/grid/AIS error), one-to-one Hungarian/greedy assignment per pass, length cross-validation hook. Outputs residual-dark density grid + per-zone (12/24nm/EEZ + sub-zone) daily series for changepoint analysis. See root CLAUDE.md for full docs. | `sar_detections.json` (fallback `dark_vessels.json`), tier-1+2 tracks, `ais_snapshot.json`, `sar_detection_history.json`, `fixed_infrastructure.json` | `sar_ais_matches.json`, `sar_detection_history.json` |
| `fetch_cloudflare_radar.py` | **網路流量異常偵測（海纜中斷的「後果」訊號）** — Cloudflare Radar 時間序列（TW netflows/HTTP、AS3462 HiNet、TW IQI latency）→ 季節性去趨勢後偵測持續性偏離 → 回頭比對異常發生前 12h 在海纜旁低速滯留的船。**`location` 只吃 ISO 3166-1 alpha-2 國家碼**——曾用 3166-2 的 `TW-LIE`／`TW-KIN` 想拿馬祖金門的離島粒度，Radar 回 HTTP 400（Invalid location codes），Radar 沒有縣市粒度，別再加回來（回歸測試 `test_all_series_use_alpha2_location_codes` 守住）。偵測法：`mask_reporting_gaps`（**剛好等於 0 的點視為缺值**——首次真實執行就抓到全台 HTTP 請求量連兩小時剛好 0、偏離 -100%、z=-45.8 的假警報；整國請求量歸零不可能，那是回報缺口，真斷纜是深跌但非零）→ `hour_of_week_baseline`（同星期幾＋同小時的 leave-one-out 中位數，異常點不污染自己的基線；不足兩週退回 24 點滾動中位數）→ `effective_scale`（MAD×1.4826 與「基線 0.5%」取大者，兩端都要防：MAD=0 會讓偵測放棄，MAD 過小會讓 1% 抖動變異常）→ `detect_anomalies`（連續 ≥2 時段 **且** 相對偏離 ≥10%；單點尖刺是雜訊，海纜中斷的特徵是持續性位階下移）。嚴重度由幅度×時數決定（≥30%且≥3h→critical）。`correlate_with_vessels` 用 0.1° 海纜格網索引把逐點距離計算降到 O(1) 預篩（數萬軌跡點 0.05s），並沿用 `analyze_suspicious.check_exclusion_rules`（濾掉漁網信標——海纜旁最密的低速目標）＋ in-port 抑制（海纜登陸點就在港邊），候選依「船型威脅權重→距海纜→出現次數」排序，`candidate_summary` 分層計數（商船／公務船／其他）讓人一眼看出有沒有實質線索。Token 環境變數接受 `CLOUDFLARE_API_TOKEN` 與別名 `CLAUDEFARETOKEN`/`CLAUDEFLARETOKEN`/`CF_API_TOKEN`（Radar 端點不需要 account ID）。**關聯不是因果**：真正的破壞者可能關了 AIS，那要靠 SAR 暗船那條線。**執行節奏**：專屬的 `cloudflare-radar.yml` 每 2 小時跑一次（海纜中斷要在數小時內知道，跟著 12 小時的主管線太慢），與其他管線共用 `data-pipeline` concurrency group 避免 push 互相踩踏。**只提交 `docs/cf_radar.json` 一份**、且輸出前用 `trim_series_for_output()` 把原始陣列裁到最近 14 天並取 2 位小數（偵測仍用完整 28 天視窗）——這個檔一天被重寫 12 次，未裁切的 6 條序列 ×28 天約 169KB，兩份一起提交一年會替 repo 累積數百 MB（本專案已被 `vessel_routes` 撐爆過一次）。`data/cf_radar.json` 因此列入 `.gitignore`，`generate_dashboard` 找不到它時會退回讀 `docs/` 那份。`correlation_coverage` 區分「比對過但沒有嫌疑船」與「**比不到**」：tier-1 軌跡只留 14 天而 Radar 視窗是 28 天，更早的異常標記為 `outside_ais_window`（summary 另計 `anomalies_outside_ais_window`），前端顯示「此事件早於 AIS 軌跡保留範圍」而不是「無候選」——查不到不等於沒有。Tests: `tests/test_cloudflare_radar.py`（44 項純函式，含雜訊序列不誤報＋雜訊中仍抓到 50% 中斷＋零值假警報＋alpha-2 回歸＋涵蓋範圍標記）。 | Cloudflare Radar API, tier-1+2 tracks, `cable-geo.json` | `docs/cf_radar.json` |
| `anomaly_detect.py` | **時間序列異常偵測的共用核心**（Cloudflare Radar 與 IODA 兩條線共用，判讀才不會各自漂移）。`mask_reporting_gaps`（0 值＝回報缺口）→ `hour_of_week_baseline`（同星期幾＋同小時的 leave-one-out 中位數）→ `effective_scale`（MAD×1.4826，下限＝基線 0.5%）→ `detect_anomalies`（連續 ≥2 時段 **且** 相對偏離 ≥10%）→ `classify_severity`（幅度×時數）。`analyze_values(timestamps, values, direction)` 是來源無關的完整流程入口；`trim_series_for_output()` 供輸出端裁短陣列。原本寫在 `fetch_cloudflare_radar.py` 裡，接 IODA 時抽出來。 | — | — |
| `fetch_ioda.py` | **離島網路可達性監測（金門／馬祖／澎湖）** — Cloudflare Radar 的 `location` 只吃 alpha-2 國碼，量不到縣市；IODA（Georgia Tech）提供 **region 級**訊號，正好補上這個粒度。每島抓三種**互相獨立**的來源：`bgp`（前綴是否從路由表消失，控制面）／`ping-slash24`（分散式主動探測，資料面）／`merit-nt`（darknet 背景流量，被動面）。Region 代碼以 `/entities/query` **動態解析**（金門 4209、連江 4210），不寫死。`annotate_corroboration()`：**只有單一來源異常的 critical 會降級為 high** —— 三種來源原理完全不同，任一種單獨下掉多半是該來源自己的量測問題，兩種以上同時下掉才值得當成真中斷。船隻關聯收窄到該島 `ISLAND_RADIUS_KM`=120km 內（全台範圍找出來的船跟馬祖斷線八竿子打不著），其餘沿用 `fetch_cloudflare_radar.correlate_with_vessels`。**馬祖的判讀特別注意**：它有微波備援，海纜斷掉不會歸零而是嚴重降速，所以用「相對自身基線的持續性偏離」而非「歸零」。不需要任何憑證。`--dump-raw` 印出原始回應供除錯。Tests: `tests/test_fetch_ioda.py`（16 項；`entities/query` 的回應形狀是實測抓回來的，`signals/raw` 的形狀沙箱連不上無法驗證，因此解析寫成多層包裝容錯）。 | IODA API, tier-1+2 tracks, `cable-geo.json` | `docs/ioda.json` |
| `fetch_weekly_dark_vessels.py` | Extract 90-day SAR dark vessel data grouped by date | GFW API | `weekly_dark_vessels.json` |
| `detect_ship_transfers.py` | Detect ship-to-ship rendezvous (<10m, >1hr), classify as pair trawling vs suspicious. **Merges tier-1 + tier-2 tracks by exact timestamp** (`load_merged_snapshots()`) so tanker↔tanker STS (shadow-fleet transfers) is detectable — tier-1-only would never see commercial vessels. | `ais_track_history.json` (tier-1), `ais_track_commercial.json` (tier-2), `ais_snapshot.json` | `ship_transfers.json` |
| `analyze_suspicious.py` | **Core threat scoring engine** — see root CLAUDE.md for full scoring docs | profiles, tracks (tier-1+2), cables, identity events, sanctions, MARS cache, STS transfers | `suspicious_vessels.json` |
| `exercise_prediction.py` | Correlate dark vessel activity with PLA sortie data (Granger causality) | `dark_vessels.json`, PLA sortie data | `exercise_prediction.json` |
| `extract_all_routes.py` | Batch extract per-vessel route JSONs from tier-1+tier-2 track history | `ais_track_history.json`, `ais_track_commercial.json` | `data/vessel_routes/{mmsi}.json`（gitignored on main；CI 以單一 commit force push 到 `vessel-data` 分支）|
| `generate_dashboard.py` | Consolidate all data → single JSON for frontend; copy auxiliary files to docs/. Also refreshes `vessel_monitoring.daily` dark-vessel trend from `dark_vessels.json` and accumulates it in `data/dark_vessel_history.json` (persistent, max 365 days) so the trend never freezes. Embeds `cf_radar.json` as `network_anomalies` **minus the raw arrays** (`timestamps`/`values`/`baseline` are stripped — 672 points × 6 series would bloat `data.json`; only metadata + anomaly events + outage annotations go in). | All `data/*.json` | `docs/data.json` + copies to docs/, `data/dark_vessel_history.json` |
| `generate_report.py` | **每日情報報告頁** (runs right after `generate_i18n_pages.py`) — bilingual shareable page + machine-readable JSON + report index. Beyond the 6 headline stat cards it renders **暗船比對成效** (SAR×AIS funnel: dark_total → infra filtered → locally re-matched → residual, plus false-dark removal %) and **SAR 取證影像** (up to `FEATURED_MAX`=6 Sentinel-1 chips from the latest forensics run with verdict badges, estimated length/peak ratio, + the run's full verdict table). Verdict keys come from `darkship_batch.verdict_key()` — the one source of truth. Chip originals (1.5MB each, 232MB total) stay out of the Pages artifact: `make_thumbnails()` writes 520px/q72 JPEGs to `docs/reports/chips/` and `prune_thumbnails()` drops them after 14 days **by mtime** (the filename date is the SAR acquisition date, ~a month older than the run, so pruning by it would delete fresh thumbs); `<img onerror>` falls back to the raw.githubusercontent original, so old report pages keep working. Pillow is optional — without it the page just links the originals. `og:image` becomes the day's featured chip. | `docs/data.json`, `docs/ship_transfers.json`, `docs/sar_ais_matches.json`, `chips/results.json` | `docs/reports/<date>.{html,json}`, `docs/reports/index.html`, `docs/reports/chips/*.jpg` |

### Utilities

| File | Purpose |
|------|---------|
| `build_chip_worklist.py` | **Chip-retrieval worklist (in CI)** — cross residual dark detections in the focus areas (`WORKLIST_ZONES`: east + southwest Taiwan) with Sentinel-1 IW GRDH product footprints from CDSE OData (anonymous, one bbox query for the window) → per-target covered/no-coverage status, exact acquisition time, and a ready-to-copy `fetch_sar_chip.py` command. Sentinel-1 coverage east of Taiwan is sparse (open-ocean IW acquisitions are infrequent), so pre-checking spares manual date-guessing. Output copied to docs/ and rendered as the report page's 取證清單 panel. | `sar_ais_matches.json`, CDSE OData | `sar_chip_worklist.json` |
| `lookup_itu_mars.py` | ITU MARS ship station registry scraper. CLI: `python3 lookup_itu_mars.py <MMSI>`. Batch mode with 2s rate limit. Cache in `data/itu_mars_cache.json` (30-day expiry). |
| `fetch_sar_chip.py` | **Local evidence tool (not in CI)** — pull a Sentinel-1 GRD chip around a residual-dark detection from CDSE S3 for manual confirmation. CLI: `python3 fetch_sar_chip.py <lat> <lon> <date> [--time HH:MM] [--size-km 8] [-o out.png]`. Anonymous OData catalogue lookup → S3 windowed read of the VV GeoTIFF (range requests, a few MB) → GCP affine + nearest-GCP residual correction for lat/lon→pixel (~100m) → median+8×MAD bright-target extraction with rough length estimate → annotated PNG (dB scale, crosshair, 1km scale bar). Needs `CDSE_ACCESS_KEY`/`CDSE_SECRET_KEY` env + `pip install boto3 rasterio numpy matplotlib`. Zero repo-local imports — runs as a single downloaded file; full walkthrough in root `SAR_CHIP_GUIDE.md`. |
| `darkship_batch.py` | **Darkship SAR forensics batch runner (in CI, `darkship-cron.yml` daily 22:00 UTC)** — automates the local forensics workstation's standard flow: read the chip worklist (local checkout `data/sar_chip_worklist.json` by default — avoids GitHub Pages CDN staleness; URLs also accepted) → skip already-done targets via `chips/results.json` (dedupe key lat/lon/date) → run `fetch_sar_chip.py` per target (newest first, default 10/run, stop after 3 consecutive failures) → parse its stdout (`parse_fetch_output`, pure fn) → write `reports/darkship_report_<date>.md` with per-target verdicts (✅ confirmed physical target = found & peak ≥10×, 🟡 weak, ⚠️ land/structure, ⚪ no target). `verdict_key()` (pure, keys `confirmed`/`weak`/`land`/`none`/`error`) + `VERDICT_LABELS` are the **single source of truth** for those verdicts — consumed by `generate_report.py`'s forensics section and mirrored by `chipVerdictKey()` in `docs/js/map-vessels.js`. Needs `CDSE_ACCESS_KEY`/`CDSE_SECRET_KEY`. Results are committed to the repo by the workflow. Tests: `tests/test_darkship_batch.py`. |
| `extract_vessel_route.py` | CLI utility to extract single vessel route by MMSI |
| `map_basemap.py` | **Shared basemap layers for the matplotlib maps** (`plot_gov_vessel_tracks.py`, `publish_threads.generate_track_map`). `draw_land(ax, bounds)` renders real coastlines from the committed `data/land_basemap.geojson` (Taiwan + China coast + Penghu/Kinmen/Matsu), view-filtered by bbox; falls back to the old hand-written `TAIWAN_COASTLINE` outline if the asset is missing. `draw_cables(ax, bounds)` renders submarine cables from `data/cable-geo.json` (fallback `docs/taiwan_cables.json`) — the cable GeoJSON's features are all **MultiLineString**, which the gov-track map's own loader (LineString-only) silently dropped, so that map's cable layer had always been empty despite the legend. Both loaders cache module-level. Tests: `tests/test_map_basemap.py` (stub `ax`, no matplotlib needed). |
| `build_land_basemap.py` | **One-off asset builder (not in CI)** — clips Natural Earth 1:10m land (public domain, 10MB world file whose Eurasia landmass is one giant polygon) to the monitoring area via Sutherland–Hodgman, drops interior rings/slivers, rounds to 3 decimals → `data/land_basemap.geojson` (136 KB, 208 polygons, committed). Clip box (17–32°N, 114–132°E) is wider than `TAIWAN_BBOX` so a map view never shows a flat cut-off coastline. Coastlines don't move — rerun only to change resolution or extent. CLI: `python3 src/build_land_basemap.py [--source ne_10m_land.geojson] [-o out.geojson]`. |
| `plot_gov_vessel_tracks.py` | Scan `data/vessel_routes/` for China gov / special-interest vessels (海警/海巡/海救/科研) and render their combined 14-day tracks to a dark-themed PNG, colored by sub-category, with place-name labels + per-track vessel name/position. CLI: `python3 plot_gov_vessel_tracks.py [-o out.png]` (default `docs/cn_gov_vessel_tracks.png`). Requires matplotlib (CJK labels need WenQuanYi/Noto, else fall back to DejaVu). **Runs once daily (00:00 UTC) in `update-data.yml`** — committed & deployed with the rest of `docs/`. Land + cable layers come from `map_basemap.py` (real coastlines, not the old hand-drawn Taiwan outline). `plot_tracks()` also takes optional `title` / `max_labels` / `pad` / `include_bounds` so `gov_daily_activity.py` can reuse it for the single-day 海警 map — the daily map passes `TAIWAN_VIEW_BOUNDS` so Taiwan is always in frame (two ships moored off Xiamen would otherwise zoom the view down to a 1° box with no reference point). |
| `build_sanctions_blacklist.py` | Convert `data/sanctions_blacklist.csv` (multi-agency shadow-fleet tanker list: OFAC/UK-FCDO/UANI/EU/SECO/MFAT columns of designation dates) → `data/sanctions_blacklist.json`, a second sanctions source consumed by `analyze_suspicious.py` (**IMO-match only**, +8; identity-concealment flag when AIS name ≠ registered name). Both CSV + JSON are committed static inputs; rerun after updating the CSV. |
| `generate_og_image.py` | Generate the 1200×630 social-share banner → `docs/og-banner.png`, used site-wide as `og:image` / `twitter:image`. Renders a **real map** of the monitored area: Taiwan land from `docs/data/taiwan_land.geojson` (detailed g0v coastline incl. Penghu/Kinmen/Matsu; falls back to `territorial_baseline.json`), real submarine-cable routes (`data/cable-geo.json`), live vessel positions + suspicious vessels (`docs/data.json`), with a left dark-gradient panel for the zh+en title. Committed static asset (not CI-regenerated); rerun to refresh the map or change branding. Needs matplotlib + a CJK font (WenQuanYi/Noto). CLI: `python3 generate_og_image.py [-o docs/og-banner.png]`. `taiwan_land.geojson` was decoded once from g0v/twgeojson `twCounty2010.topo.json` (43 land polygons). |
| `generate_summary.py` | Generate daily/weekly text summary reports |
| `publish_threads.py` | Generate charts + maps, publish to Threads (requires `THREADS_*` secrets) |
| `SendMessage.py` | **LINE Bot 每日推送** (`LINEBot.yml`, 00:00 UTC = 08:00 TW) — one text message + up to 2 images: (1) **昨日海警船海上動態** via `gov_daily_activity.py` (逐船點名 + 動態圖; berthed vessels excluded), (2) **本週危險係數最高的商船** (`select_top_commercial_vessel`: cargo/tanker/lng sorted by `cable_details.loiter_slow_hours` then risk score, falls back to the top-scoring suspicious vessel) + its track map from `data/vessel_routes/{mmsi}.json` (fetched from the `vessel-data` branch by the workflow; needs ≥15 points). Text comes from Gemini (`GEMINI_API_KEY`) with a fixed-template fallback; images are uploaded to `data/charts/` via the GitHub Contents API to get public raw URLs (needs `GITHUB_TOKEN`, else text-only). `--dry-run` prints the report + writes the images without pushing. |
| `gov_daily_activity.py` | **昨日中國公務船單日海上動態** — slices one Taiwan calendar day (`tw_day_window`, days_back=1) out of tier-1 tracks and aggregates per vessel: 出現時段, 位置筆數, 最新位置, 最高/平均速度, 日內移動距離 (`moving` = ≥5km), and (via `annotate_zones` → `geofence.classify_maritime_zone`) the **closest approach to Taiwan's baseline** in nm + its zone — the gray-zone pressure signal. **Berthed positions are not activity**: every point is tested with `port_lookup` (default `geofence.is_in_port_cached`, TW ports 2km / CN ports 8km+), `points` keeps only the at-sea ones, and a vessel that never left port gets `at_sea=False` — `at_sea_only()` drops those from both the report and the map (a typical day is ~24 gov vessels of which ~10 sit in Xiamen/Fuzhou all day). For a sortie (port → sea → port) all metrics are computed over the at-sea leg only. Category comes from the tier-1 `gov:<category>` flag, then `type_name`, then `classify_gov_vessel(name)`; MMSI starting with `9` (AtoN/base station) is dropped. `summarize_activity()` names 海警 vessels individually (default `MAX_DETAIL_VESSELS`=6) and reports only counts for 海巡/海救/科研 (`note_in_port=True` adds an "N 艘停泊港內" line — CLI debugging only, the push report never mentions port activity). `build_daily_gov_map()` renders **all** at-sea gov vessels colored by category (海警 sorted first so it wins the label quota — on days with one 海警 at sea a coastguard-only map would be a near-empty frame) through `plot_gov_vessel_tracks.plot_tracks(title=…, max_labels={coastguard:6, others:2}, pad=…, include_bounds=TAIWAN_VIEW_BOUNDS)` — real coastline + submarine cable basemap, Taiwan always in frame, English title (survives a runner with no CJK font). Aggregation is pure (`port_lookup`/`classifier` injectable, no file/matplotlib deps) — tests: `tests/test_gov_daily_activity.py`. CLI: `python3 src/gov_daily_activity.py [--days-back N] [-o out.png]`. |
| `generate_i18n_pages.py` | Build `docs/en/` English mirrors of the static content pages (`STATIC_PAGES`) for true URL-level i18n. Per page: forces `<html lang="en">` + `<body class="lang-en">`; rewrites `<head>` metadata + canonical/og:url from `EN_META`; localizes **every JSON-LD block** (parses JSON — FAQPage→`EN_FAQ`, Article/Breadcrumb/ItemList/HowTo/DefinedTerm→English via `EN_META`/`EN_BREADCRUMB_*`/`EN_HOWTO`, adds `mainEntityOfPage`+`speakable`, `inLanguage`→`en`, BASE urls→`/en/`); emits an **English-only DOM** (resolves `data-i18n` text from `js/i18n.js`, strips `lang-zh-only` elements depth-aware, translates language-neutral chips/tags via `EN_TEXT`, drops glossary `g-zh` labels, flips the toggle to `中`). Idempotent — rebuilds `docs/en/*.html` each run. **Runs in `update-data.yml`** after `generate_dashboard.py`. Regression gate: `tests/test_i18n_pages.py`. When adding a new bilingual evergreen article, add it to `STATIC_PAGES` + `EN_META` (+ `EN_FAQ`/`EN_BREADCRUMB_LAST` if it has FAQ/breadcrumb), the `blog.html` card grid, and `docs/sitemap.xml` (zh + en). |

### Bilingual evergreen articles (`docs/blog-*.html`)
Static explainer/pillar pages carry both languages inline (`lang-zh-only` / `lang-en-only` blocks + `data-i18n` spans) and toggle client-side; their English `/en/` URLs are generated by `generate_i18n_pages.py`. The glossary (`blog-gray-zone-glossary.html`) is a bilingual `DefinedTermSet`; its terms carry `seeAlso` links to the relevant explainers.

**Evergreen citation standard** — every evergreen article must have (enforced by `tests/test_i18n_pages.py::test_all_evergreen_pages_have_sources_and_citation` + `..._topic_cluster_and_dates`):
1. visible `.ai-summary` GEO abstract;
2. FAQ (`<details>` + `FAQPage` JSON-LD);
3. `series-nav` RELATED rail + shared bilingual `.topic-cluster` footer (before `<footer>`);
4. visible **Sources** section (`<section id="sources">`, bilingual) listing authoritative external sources;
5. Article(+TechArticle) JSON-LD with `mainEntityOfPage`, `speakable` (`.ai-summary`), `datePublished` **and** `dateModified`, `author`/`publisher` (Organization), `about`, and a **`citation`** array of authoritative sources (absolute URLs); data-derived pages (e.g. methodology) also add `isBasedOn` → `data.json`;
6. a limitations/disclaimer note (signals are leads, not legal/factual findings).
The `/en/` mirror strips the Chinese half so Sources render English-only; external citation/source URLs stay absolute.

## Key Data Structures

### Vessel Profile (`vessel_profiles.json`)
```python
{
    "412345678": {
        "mmsi": "412345678",
        "names_seen": ["SHIP A", "SHIP B"],
        "types_seen": ["fishing", "cargo"],
        "total_snapshots": 42,
        "fishing_hotspot_snapshots": 15,
        "last_seen_timestamps": ["2024-01-01T00:00:00+00:00", ...],  # last 50
        "last_imo": "9876543",
        "last_callsign": "BXYZ",
    }
}
```

### Track Entry (both tier-1 and tier-2)
```python
{
    "timestamp": "2024-01-01T00:00:00+00:00",
    "vessel_count": 150,
    "vessels": [
        {"mmsi": "412345678", "name": "SHIP A", "lat": 24.5, "lon": 121.0,
         "speed": 8.5, "heading": 180.0, "type_name": "fishing"}
    ]
}
```

### Track Tiers
- **Tier-1** (`docs/ais_track_history.json`): CN fishing + suspicious vessels, max 168 entries (14 days). Used by analyze_suspicious / detect_ship_transfers / route extraction / per-MMSI route lookups; kept compact (rounded coords, no per-vessel `suspicious` flag). Suspicion is resolved frontend-side from `data.json` (`suspicious_analysis`).
- **Animation subset** (`docs/ais_track_animation.json`): last 7 days of tier-1, written alongside it by `fetch_ais_data.py` (`AIS_TRACK_ANIMATION_DAYS = 7`). The animation pages fetch this first and fall back to the full tier-1 file — roughly halves the animation download.
- **Tier-2** (`docs/ais_track_commercial.json`): cargo/tanker/LNG + identity-changed, max 336 entries (28 days)
- Identity-changed MMSIs loaded from `data/identity_events.json` (last 7 days)
- Large accumulating files (`vessel_profiles.json`, `ais_track_history.json`, `ais_track_commercial.json`) are written to `docs/` as compact JSON (no indent) to stay under GitHub's 100 MiB per-file limit. `data/` counterparts are gitignored.

## Conventions
- All timestamps ISO 8601 UTC
- MMSI as strings (preserve leading zeros)
- Monitoring bbox (`TAIWAN_BBOX` in `fetch_ais_data.py`): lat 19-30°N, lon 116-130°E
- Track points deduplicated by consecutive identical lat/lon
- Port coordinates (`PORTS` Taiwan commercial+fishing ports, `CN_PORTS` CN coastal ports/anchorages) and `is_in_port()` / `is_in_port_cached()` live in `geofence.py` — shared by `detect_ship_transfers.py` (STS port exclusion) and `analyze_suspicious.py` (in-port scoring suppression)
- `classify_vessel_type(code)` maps AIS type codes → `fishing`/`cargo`/`tanker`/`lng`/`other`/`unknown`
- China gov / special-interest vessel detection: `classify_gov_vessel(name, mmsi=None)` in `fetch_ais_data.py` returns a sub-category — `coastguard` 海警 / `msa` 海巡(海事局) / `rescue` 海救(救助局) / `research` 科研·情報船 — by name keyword first, then (on name miss) by the `KNOWN_GOV_MMSI` **exact-MMSI** table of individually-evidenced gov vessels (each entry carries an evidence comment; seed: 413875010=海警1401). MMSI-**prefix** matching remains rejected — block `413875xxx` is shared with civilian vessels (e.g. HUAHANG10DP). Additionally `is_gov_candidate(name, mmsi)` flags pure-digit names (3–5 digits) with a China MID as `gov_candidate` (tier-1 always retained, compact `cand:1` flag) for manual review without auto-classification; "contains 4 digits" is deliberately NOT a signal (MINXIAYU01401 is a fishing boat). Tests: `tests/test_gov_mmsi.py`. On match, `type_name` is overridden to the category (fixes AIS mis-reporting these as fishing/special/other), `gov_type` is set on the snapshot vessel (plus `is_coast_guard` for the coastguard sub-type); tier-1 tracks gain a compact `gov:<category>` flag and these vessels are always retained in tier-1 (so routes accumulate). Research keywords use word boundaries to avoid false hits (e.g. `AN TONG JING TANG` ≠ `TONG JI`). `is_coast_guard_vessel()` is kept as a thin compat wrapper. Taiwan CGA (海巡署) is intentionally excluded.
